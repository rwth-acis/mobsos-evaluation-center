import { VisualizationDirective } from './visualization.directive';

describe('VisualizationDirective', () => {
  it('should create an instance', () => {
    const directive = new VisualizationDirective();
    expect(directive).toBeTruthy();
  });
});
