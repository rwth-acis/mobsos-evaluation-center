name: Lint 

on:
  pull_request:
    branches: [ master, develop ]

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
    - name: Lint 
      run: npm run lint
  test-build:
    runs-on: ubuntu-latest
    needs: [lint]
    steps:
      - name: Test build 
        run: npm i && npm run build